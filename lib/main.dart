import 'package:flutter/material.dart';

int globalId = -1;

void main() {
  runApp(MaterialApp(
    debugShowCheckedModeBanner: false,
    // initialRoute: Login.routeName,
    // routes: routes,
  ));
}
